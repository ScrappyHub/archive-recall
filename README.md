# archive-recall
Canonical library preservation + deterministic restore system. Content-addressed storage, SHA-256 integrity, append-only action journal, approved transformers, reversible load pipelines, and policy-governed importers.
